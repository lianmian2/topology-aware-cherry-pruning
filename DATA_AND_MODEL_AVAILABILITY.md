# Data and model availability

The `v1.0` GitHub Release is being prepared. It will contain the sanitized RGB images, annotations, split manifests, derived data, frozen checkpoints, output tables, data dictionary, and checksum manifest under CC BY 4.0.

No dataset or trained checkpoint is included in this private pre-publication code repository. They are attached to the corresponding `v1.0` GitHub Release.
